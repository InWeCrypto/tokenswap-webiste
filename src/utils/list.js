import http from './ajax';

(function(win, http) {
    var defaultOptions = {};
    function OrderList (){

    };
    OrderList.prototype = {
        
    }
    win.orderList = OrderList
})(window, http);

